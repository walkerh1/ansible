- name: install apps
  community.general.homebrew_cask:
    name: "{{ applications }}"
    state: present
    update_homebrew: true
  vars:
    applications:
      - firefox
      - ghostty
      - obsidian
      - podman-desktop
      - rectangle
      - spotify
      - syncthing
      - zed
  tags:
    - apps
