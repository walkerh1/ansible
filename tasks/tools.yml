- name: install tools
  become: true
  community.general.homebrew:
    name: "{{ packages }}"
    state: latest
    update_homebrew: true
  vars:
    packages:
      - bat
      - fd
      - fzf
      - golang
      - jq
      - neovim
      - ripgrep
      - tmux
      - tree
  tags:
    - tools
