- name: install tools
  become: true
  dnf:
    update_cache: yes
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
      - bat
      - dnf-plugins-core
      - fd-find
      - fzf
      - golang
      - jq
      - ripgrep
      - tmux
      - tree
  tags:
    - tools
