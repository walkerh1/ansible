- name: install tools
  become: true
  dnf:
    update_cache: yes
    name: "{{ packages }}"
    state: latest
  vars:
    packages:
      - dnf-plugins-core
      - ripgrep
      - fzf
      - tree
      - bat
      - tmux
  tags:
    - tools
